import psycopg2

DB_NAME = "hollywood"
USER = "postgres" 
PASSWORD = "postgres" 
HOST = "localhost"
PORT = "5432"

connection = psycopg2.connect(host=HOST, user=USER, password=PASSWORD, dbname=DB_NAME)
cursor = connection.cursor() 

query = 'SELECT * FROM actors LIMIT 20;' #add ;

cursor.execute(query) # execute the query

def create_table(oscars: str): 
    """create new table with id, num""" # ' ' = single line where  ''' ''' = multi line strings

    query = f'''
    create table {oscars}(
        id serial primary key,
        num integer not null
    );
    '''
    cursor.execute(query) # execute the query
    connection.commit() # make changes in the database